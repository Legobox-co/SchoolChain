// import { login, logout, setRedirectUrl, signup, autoLogin } from './Auth';
// import API from '../../Utils/api';
// import env from '../../env';
// import { changeProject } from './Project';

// const api = new API({
// 	baseURL : env.API_BASE_URL,
// })

// api.setHeader('accept', 'application/json')
// api.setHeader('X-Requested-With', 'XMLHttpRequest')

// export {api}; 

export default {
	// login,
	// logout,
	// signup,
	// setRedirectUrl,
	// autoLogin,
	// changeProject
}